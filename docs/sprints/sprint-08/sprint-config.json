{
  "sprint_id": "sprint-08",
  "status": "deleted",
  "deleted_on": "2026-05-19",
  "deleted_reason": "Sprint 07 scoped down to boot-waterfall-only viewer (no spawn/kill buttons). UI is no longer a control surface — it's a read-only observability view. Spawn/kill functionality, if needed, lives in the rfa CLI alone. This sprint's scope (UI spawn/kill buttons) is no longer needed.",
  "objective": "deleted — see deleted_reason",
  "in_scope": [],
  "out_of_scope": ["everything"],
  "exit_criteria": ["this sprint does not run"]
}
